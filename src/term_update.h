#ifndef TERM_UPDATE_H
#define TERM_UPDATE_H
#include <stdio.h>
#include <string.h>
#include <curses.h>

//int term_print(char *working_directory, char *term_string, WINDOW *term_panel) {
  //char *log_path; int i;
  //sprintf(log_path,"%s/term.log", working_directory);
  //FILE *log_file = fopen("disp/home/sigma","w");
  //fprintf(log_file,"%s", term_string);
  //fclose(log_file);

  //for (i = 1; i < term_panel_height; i++) {
    //if ( i == term_panel_height-1 ) {
    
    //}

    //if ( mvwinch(term_panel,i,1) == " " ) {
    //    mvwprintw(term_panel,i,1,term_string);
    //  break;
    //}
  //}

  //return 0;
//}

#endif /* TERM_UPDATE_H */
